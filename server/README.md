# Chat Server

GraphQL Yoga server implementation supporting subscriptions via `graphql-ws`.

## Scripts
- `npm run dev`: Runs the server with `tsx` watch mode.
- `npm run build`: Compiles the server using `tsc`.
- `npm start`: Runs the production server build.
