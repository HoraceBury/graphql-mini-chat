# Chat Client

Node.js CLI chat client using `graphql-ws` to communicate with the server.

## Scripts
- `npm run dev`: Runs the client with `tsx`.
- `npm run build`: Compiles the client using `tsc`.
- `npm start`: Runs the production client build.
